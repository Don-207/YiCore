# CH32H417 evaluation board

The initial target is the CH32H417QEU evaluation board running only the V3F
core. It assumes the vendor reference-board 25 MHz HSE and toggles PB1 as an
active-high heartbeat output.
